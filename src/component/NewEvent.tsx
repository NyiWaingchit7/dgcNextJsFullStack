import { Box } from "@mui/material";
interface Props {
  open: boolean;
  setOpen: (data: boolean) => void;
  id?: number;
}
const NewEvent = () => {
  return <Box></Box>;
};

export default NewEvent;
