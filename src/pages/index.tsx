import { Box, Typography, Button } from "@mui/material";
import { signIn, useSession } from "next-auth/react";
import { useRouter } from "next/router";

const App = () => {
  return <Box>This landing page</Box>;
};
export default App;
